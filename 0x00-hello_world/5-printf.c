#include <stdio.h>
/**
 * main - Entry point
 * Printf - Print textlines and Numbers on screen
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
